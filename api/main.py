"""
WEEK 4 — this stays empty until weeks 1-3 are done.

This file will wrap the finished agent system in a FastAPI app, so instead
of only running from a script, it can be called over HTTP — which is what
lets it be containerized and deployed (Render).
"""
