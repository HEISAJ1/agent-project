"""
WEEK 3 — this stays empty until the agent actually works end-to-end.

This file will hold 20-30 test cases used to check the agent isn't just
"working when I try it" but actually working reliably. Each test case will
check things like:
  - did the agent call the correct tool for this task?
  - did it stop instead of looping forever?
  - does its final answer pass a quality rubric (scored by another LLM call)?

We run this suite with DeepEval, and later wire it into GitHub Actions so it
runs automatically on every push.
"""
